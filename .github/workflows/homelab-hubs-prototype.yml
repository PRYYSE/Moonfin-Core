name: Home Lab Web UI (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - name: Development moved to docker01
        run: |
          echo "Home Lab UI development now uses the persistent source tree on docker01."
          echo "This workflow is intentionally manual-only until the redesigned Dart source is committed and a simple validation workflow is restored."
